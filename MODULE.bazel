module(name = "sonic-buildimage")

bazel_dep(name = "bazel_skylib", version = "1.9.0")
bazel_dep(name = "rules_oci", version = "2.2.6")
bazel_dep(name = "tar.bzl", version = "0.10.5")
bazel_dep(name = "rules_python", version = "1.7.0")
bazel_dep(name = "bazel_lib", version = "3.1.1")
bazel_dep(name = "pigz", version = "2.8")
bazel_dep(name = "aspect_rules_py", version = "1.11.2")

bazel_dep(name = "buildifier_prebuilt", version = "8.5.1.3", dev_dependency = True)

python = use_extension("@rules_python//python/extensions:python.bzl", "python")
python.toolchain(python_version = "3.11.6")

bazel_dep(name = "rules_distroless", version = "0.0.0.commit-01bb79d7eef4eb3ce9511d3dfaf0ab386bd8d12d")

# Dependencies from this repository's submodules.
# Versions here are irrelevant (see root-unpinned-modules-config.bazelrc).
bazel_dep(name = "sonic-build-infra", version = "0.0.0", repo_name = "sonic_build_infra")
bazel_dep(name = "sonic-sysmgr", version = "0.0.0", repo_name = "sonic_sysmgr")
bazel_dep(name = "sonic-swss-common", version = "0.0.0", repo_name = "sonic_swss_common")

toolchains = use_extension("@tar.bzl//tar:extensions.bzl", "toolchains")
use_repo(toolchains, "bsd_tar_toolchains")

register_toolchains("@bsd_tar_toolchains//:all")

# The actual dependencies are listed in src/sonic-build-infra/MODULE.bazel, and re-exported.
apt = use_extension("@rules_distroless//apt:extensions.bzl", "apt")
use_repo(apt, "trixie")

uv = use_extension("@aspect_rules_py//uv/unstable:extension.bzl", "uv")
use_repo(uv, "pypi")

http_archive = use_repo_rule("@bazel_tools//tools/build_defs/repo:http.bzl", "http_archive")

# elfcompare, which reports the semantic differences between two ELF artifacts.
# The upstream sources carry no build files, so both the BUILD file and the
# patch that lets the tools be selected through the environment come from here.
http_archive(
    name = "compare_elf",
    build_file = "//tools/bazel:compare_elf.BUILD.bazel",
    patch_args = ["-p1"],
    patches = ["//tools/bazel:compare_elf_env.patch"],
    sha256 = "e77de8666533ef29a97064a55234c2ae54526a82cda32428dc65c13caea9172e",
    strip_prefix = "compareELF-5f79c6208ccaad40d8c769304b8148f8c07a901e",
    urls = ["https://github.com/dzfdzfdzf/compareELF/archive/5f79c6208ccaad40d8c769304b8148f8c07a901e.tar.gz"],
)
