# TODO(bazel-ready): This assumes this repo is a subdirectory of sonic-buildimage.
common --registry=file://%workspace%/../../tools/bazel/registry
common --registry=https://raw.githubusercontent.com/blorente/sonic-bazel-registry/main
common --registry=https://bcr.bazel.build
